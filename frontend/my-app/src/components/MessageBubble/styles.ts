import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  message: {
    padding: 10,
    margin: 8,
    backgroundColor: "#eee",
    borderRadius: 8,
    alignSelf: "flex-start",
  },
});