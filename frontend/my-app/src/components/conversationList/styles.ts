import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  chatItem: {
    padding: 15,
    borderBottomWidth: 1,
    borderBottomColor: "#eee",
  },

  chatName: {
    fontSize: 16,
  },
});