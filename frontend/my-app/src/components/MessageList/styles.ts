import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  dateContainer: {
    alignItems: "center", 
    marginVertical: 15,
    backgroundColor: "#eee",
    borderRadius: 8,
  },
}); 